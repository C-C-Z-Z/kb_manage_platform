# M15 示例数据与初始化 模块 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 模块编号 | M15 |
| 模块名称 | 示例数据与初始化 |
| 文档版本 | v1.0 |
| 目标读者 | 产品、研发、测试、运维、实施 |
| 上游依据 | 总体 PRD、模块级 PRD、现有代码与数据库迁移 |
| 文档状态 | 当前阶段可实现、可验收版本 |

## 2. 全局约束摘要

- 认证方式：Bearer JWT，默认有效期 8 小时。
- 权限模型：操作权限 RBAC + 知识四维数据权限。
- 四维权限：global、department、role、user，按 OR 逻辑组合。
- API 前缀：`/api/v1`。
- 后端分层：API / Service / Domain / Infrastructure / Bootstrap。
- 事实存储：MySQL、Redis、MinIO、Milvus。
- 本地向量模型：BGE-M3，默认 CPU，FP16=False。
- 所有知识访问必须实时执行数据权限校验。
- 密码、Token 和模型密钥不得写入日志。
- 固定示例数据必须使用稳定 ID，并支持幂等执行。

## 3. 模块需求

### 目标
为开发、演示和验收快速提供可操作数据。

### 初始化内容
- 部门：总公司、人力资源部、产品研发部、财务部、运营中心。
- 分类：财务制度、人力资源、产品与研发、运营管理、通用制度。
- 示例知识：差旅报销、年假、版本发布、信息安全和草稿。
- 已发布 FAQ、候选 FAQ、知识缺口。
- 示例会话和引用。
- 近 7 天问答与 Token 审计。
- BGE-M3 向量写入 Milvus。

### 执行命令
```powershell
docker compose --env-file backend\.env -f compose.yaml exec -T backend /app/.venv/bin/python -m kb_manage_platform.bootstrap.sample_data
```

### 验收标准
- 重复执行不产生重复固定 ID 数据。
- 示例知识可以预览 Markdown 和查看切片。
- FAQ 页面、缺口页面、会话和看板均有数据。

---

## 4. 依赖与交付说明

- 本模块的接口均以 `/api/v1` 为统一前缀。
- 权限码、资源状态和接口字段发生变化时，必须同步更新总体 PRD、迁移和前端权限。
- 模块验收应同时覆盖成功路径、权限拒绝、参数错误和依赖异常。
- 示例数据或初始化模块发生变化时，必须保持固定 ID 和幂等性。
